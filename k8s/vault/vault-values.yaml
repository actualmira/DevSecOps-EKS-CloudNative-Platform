global:
  tlsDisable: false

injector:
  enabled: false

csi:
  enabled: false

ui:
  enabled: false

server:
  serviceAccount:
    create: true
    name: vault

  nodeSelector:
    role: isolated

  tolerations:
    - key: "workload"
      operator: "Equal"
      value: "isolated"
      effect: "NoSchedule"

  authDelegator:
    enabled: true

  volumes:
    - name: tmp
      emptyDir: {}
    - name: vault-tls-secret
      secret:
        secretName: vault-tls-secret

  volumeMounts:
    - name: tmp
      mountPath: /tmp
    - name: vault-tls-secret
      mountPath: /vault/userconfig/vault-tls-secret
      readOnly: true

  dataStorage:
    enabled: true
    size: 10Gi
    storageClass: ebs-sc

  auditStorage:
    enabled: true
    size: 5Gi
    storageClass: ebs-sc
    mountPath: /vault/audit

  ha:
    enabled: false

  resources:
    requests:
      cpu: "50m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "256Mi"

  statefulSet:
    securityContext:
      pod:
        runAsNonRoot: true
        runAsUser: 100
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      container:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        capabilities:
          drop:
            - ALL

  livenessProbe:
    enabled: true
    failureThreshold: 5
    initialDelaySeconds: 120
    periodSeconds: 30
    timeoutSeconds: 10

  readinessProbe:
    enabled: true
    failureThreshold: 5
    initialDelaySeconds: 15
    periodSeconds: 10
    timeoutSeconds: 10

  extraEnvironmentVars:
    VAULT_ADDR: "https://vault.vault.svc.cluster.local:8200"
    VAULT_CACERT: "/vault/userconfig/vault-tls-secret/tls.crt"

  standalone:
    enabled: true
    config: |
      ui = false
      listener "tcp" {
        address = "0.0.0.0:8200"
        tls_disable = false
        tls_cert_file   = "/vault/userconfig/vault-tls-secret/tls.crt"
        tls_key_file    = "/vault/userconfig/vault-tls-secret/tls.key"
        tls_min_version = "tls13"
      }
      storage "file" {
        path = "/vault/data"
      }
      seal "awskms" {
        region     = "eu-west-1"
        kms_key_id = "alias/devsecops-dev-vault-unseal"
      }
