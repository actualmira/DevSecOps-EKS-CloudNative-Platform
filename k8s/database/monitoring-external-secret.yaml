apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mariadb-monitoring-credentials
  namespace: database
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-monitoring-secret-store
    kind: SecretStore
  target:
    name: mariadb-monitoring-secret
    creationPolicy: Owner
  data:
    - secretKey: exporter-password
      remoteRef:
        key: exporter/mariadb
        property: password
